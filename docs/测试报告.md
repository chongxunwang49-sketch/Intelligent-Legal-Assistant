# 智法通 V2 · 测试报告（部署验收）

| 项目 | 结果 |
|---|---|
| 版本 | V2.0（Docker Compose 部署） |
| 环境 | Windows 11 + Docker Desktop；容器 mysql/redis/neo4j/backend/frontend 全 healthy |
| 验收脚本 | `tests/e2e_v2.py`（结果 JSON：`tests/e2e_results.json`） |
| 结果 | **passed 24 / total 24 · 通过率 100%** |

## 1. 覆盖范围（24 项）
- 系统健康：/health healthy（15ms）
- 认证：admin/lawyer/user 三角色登录、/auth/me 权限码、无 token 401、弱密码拒绝
- RBAC：user/lawyer→/users 403；admin→/users 200；user→/analysis 403；lawyer→/analysis 200
- 知识库/两阶段检索：文档统计>0、语义检索命中
- 图谱：Neo4j 可用且节点>0、图谱可视化数据
- 时间旅行：2020-06-01 含《合同法》、2021-06-01 已剔除（精确标题判定）
- 分析/仪表盘/通知：热点、概览、未读数
- 合同：历史含演示合同、报告含风险条款
- 智能问答（非流式）：端到端 6.0s（含 GraphRAG 图谱候选 + Self-RAG 自评估 + 法条引用）

## 2. 端口与访问
前端 http://localhost:8188 · 后端 http://localhost:28000/docs · health /health

## 3. 全容器状态
zft2-mysql / zft2-redis / zft2-neo4j / zft2-backend / zft2-frontend —— 全部 `healthy`

## 4. 备注
- 智能问答质量与延迟受本地模型 qwen2.5:1.5b 限制；检索为两阶段（Chroma 粗排 + bge-reranker-v2-m3 精排，GPU）。
- 部署要点见 `docs/部署手册.md`；需求见 `docs/项目需求文档.md`。
